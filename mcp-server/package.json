{
  "name": "@stackmemory/mcp-server-cloudflare",
  "version": "2.0.0",
  "description": "StackMemory MCP Server v2.0.0 - AI Memory Management with Time Query, Clustering, Batch Ops",
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev ./dist --d1=DB",
    "build": "esbuild src/index.ts --bundle --outdir=dist --format=esm --target=esnext --sourcemap",
    "typecheck": "tsc --noEmit",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=stackmemory-mcp"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241205.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.99.0"
  }
}
