{
  "name": "@stackmemory/mcp-server-cloudflare",
  "version": "2.0.0",
  "description": "StackMemory app-first MCP adapter for forwarding MCP calls to the main application API.",
  "type": "module",
  "scripts": {
    "dev": "npm run build && wrangler pages dev ./dist",
    "build": "esbuild src/app-adapter.ts --bundle --outdir=dist --format=esm --target=esnext --sourcemap",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=stackmemory-mcp-app-adapter"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241205.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.99.0"
  }
}
